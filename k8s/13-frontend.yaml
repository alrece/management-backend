apiVersion: apps/v1
kind: Deployment
metadata:
  name: mb-frontend
  namespace: mb
  labels:
    app: mb-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mb-frontend
  template:
    metadata:
      labels:
        app: mb-frontend
    spec:
      initContainers:
        - name: wait-backend
          image: curlimages/curl:latest
          command: ["sh", "-c", "until curl -sf http://mb-system.mb.svc.cluster.local:8081/health; do echo 'waiting for backend...'; sleep 3; done;"]
      containers:
        - name: mb-frontend
          image: mb-frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: mb-frontend
  namespace: mb
spec:
  type: NodePort
  selector:
    app: mb-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
